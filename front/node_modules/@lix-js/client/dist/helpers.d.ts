import type { NodeishFilesystem } from "@lix-js/fs";
/**
 * Wraps a nodeishFs implementation with a js proxy for detailed logging, debugging and transparently replacing the file access behaviour.
 * advantage of using this approach is that the underlying fs can also be swapped to something like lightingfs seamlessly.
 */
type Args = {
    nodeishFs: NodeishFilesystem;
    verbose?: boolean;
    description?: string;
    intercept?: (args: {
        prop: keyof NodeishFilesystem;
        execute: () => any;
        argumentsList: any[];
    }) => any;
};
export declare const withProxy: ({ nodeishFs, verbose, description, intercept, }: Args) => NodeishFilesystem;
export declare function parseOrigin({ remotes }: {
    remotes?: any[];
}): string | undefined;
/**
 * Transforms a remote URL to a standard format.
 */
export declare function transformRemote(remote: string): string | undefined;
export declare function parseLixUri(uriText: string): {
    error: unknown;
    username: string;
    password: string;
    protocol: string;
    lixHost: string;
    namespace: string;
    repoHost: string;
    owner: string;
    repoName: string;
} | {
    username: string;
    password: string;
    protocol: string;
    lixHost: string;
    namespace: string;
    repoHost: string;
    owner: string;
    repoName: string;
    error?: undefined;
};
export declare function hash(inputStr: string): Promise<string>;
export {};
//# sourceMappingURL=helpers.d.ts.map