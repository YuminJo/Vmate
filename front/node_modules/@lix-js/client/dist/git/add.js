import isoGit from "../../vendored/isomorphic-git/index.js";
export async function add(ctx, state, filepath) {
    await state.ensureFirstBatch();
    return await isoGit.add({
        fs: ctx.rawFs,
        parallel: true,
        dir: ctx.dir,
        cache: ctx.cache,
        filepath,
    });
}
