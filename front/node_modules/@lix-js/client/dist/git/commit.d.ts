import type { NodeishFilesystem } from "@lix-js/fs";
import type { RepoContext, RepoState, Author } from "../openRepository.js";
export declare const isoCommit: (ctx: RepoContext, { author, message }: {
    author: any;
    message: string;
}) => Promise<string>;
export declare function commit(ctx: RepoContext, state: RepoState, { author: overrideAuthor, message, include, }: {
    author?: any;
    message: string;
    include: string[];
}): Promise<string>;
export declare function doCommit({ cache, fs, dir, ref, author, message, }: {
    cache: any;
    fs: NodeishFilesystem;
    dir: string;
    ref?: string;
    author: Author;
    message: string;
}): Promise<string>;
//# sourceMappingURL=commit.d.ts.map