import isoGit from "../../vendored/isomorphic-git/index.js";
export async function remove(ctx, state, filepath) {
    await state.ensureFirstBatch();
    return await isoGit.remove({
        fs: ctx.rawFs,
        dir: ctx.dir,
        cache: ctx.cache,
        filepath,
    });
}
