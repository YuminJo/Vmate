import isoGit from "../../vendored/isomorphic-git/index.js";
export async function log(ctx, cmdArgs) {
    return await isoGit.log({
        fs: ctx.rawFs,
        depth: cmdArgs?.depth,
        filepath: cmdArgs?.filepath,
        dir: ctx.dir,
        ref: cmdArgs?.ref,
        cache: ctx.cache,
        since: cmdArgs?.since,
        force: cmdArgs?.force,
        follow: cmdArgs?.follow,
    });
}
