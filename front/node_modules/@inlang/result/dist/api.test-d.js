import { expectType } from "tsd";
function foo() {
    return { data: "foo" };
}
const result = foo();
expectType(result);
if (result.error) {
    expectType(result.error);
    expectType(result.data);
}
else {
    expectType(result.error);
    expectType(result.data);
}
