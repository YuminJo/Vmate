import { LanguageTag } from "@inlang/language-tag";
import { Type } from "@sinclair/typebox";
export const Translatable = (type) => Type.Union([type, Type.Intersect([Type.Object({ en: type }), Type.Record(LanguageTag, type)])]);
