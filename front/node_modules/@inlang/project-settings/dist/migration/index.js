import { migrate1to2 } from "./1-to-2.js";
/**
 * Migrates the schema to the latest version if required.
 */
export function migrateIfOutdated(schema) {
    if (schema.settings) {
        return migrate1to2(schema);
    }
    return schema;
}
